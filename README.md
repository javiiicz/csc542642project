# csc542642project
Soccer World Cup Prediction Through Statistical Models

scripts in `/src/data_processing` were the ones used to create `/data/MatchData.csv`. This is the data used in `/src/lr_model.r` and `/src/svm_model.r`.
